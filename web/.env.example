# API prod (Render) — hostname seul ou URL complète ; vide en dev (proxy Vite)
# VITE_API_URL=gwriziou-api.onrender.com
# VITE_API_URL=https://gwriziou-api.onrender.com

# Sur téléphone/tablette : bloquer le navigateur et exiger l’app installée (défaut en prod)
# VITE_REQUIRE_PWA_MOBILE=false
